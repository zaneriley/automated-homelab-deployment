# Python
*.pyc
__pycache__/

# Ansible runtime
*.retry

# Legacy inventory patterns (for ansible/setup_nuc.yml; master-node inventory is tracked)
**/inventory/production
**/inventory/staging
**/inventory/*.vault
*.pem
*.key
*.tfvars
cloudflare/*.txt

# Vault password file (legacy — phase-2 migrates secrets to 1Password per ADR-0007)
.vault_pass.txt
host_key_checking

# Master-node overrides (ADR-0007 — tracked templates only; real values gitignored)
group_vars/master.yml
group_vars/nucs.yml
group_vars/nas.yml
group_vars/pis.yml
host_vars/*.yml
!host_vars/*.example.yml

# Research / scratch artifacts (literature reviews, planning docs)
.tmp/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
*.tfplan
*.tfvars.json

# macOS / editor cruft
.DS_Store
*.swp
*~
*.bak
*.log
*.tar.gz
*.zip
.vagrant/

# Secrets / credentials (catch-all — prefer 1Password references over any local file)
.secret/
secrets.yml
*secrets*
credentials
